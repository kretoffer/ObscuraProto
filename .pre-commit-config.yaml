repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v17.0.6 # Use a recent version, check pre-commit mirrors for latest
    hooks:
      - id: clang-format
        files: \.(cpp|hpp)$

